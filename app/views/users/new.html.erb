<%# A decommenter pour la méthode avec form%>
<!--
<section>
  <div class="container-fluid bg">
    <form action="/new" method="post">
      <input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>">
      Username: <br>
      <input type="string" name="user_username" ><br>
      Email: <br>
      <input type="string" name="user_email"><br>
      Bio: <br>
      <input type="text" name="user_bio"><br><br>
      <input type="submit" value="Envoyer le formulaire">
    </form>
  </div> 
</section> %>
-->

<%# A decommenter pour la méthode avec form_tag%>

<!--
<section>
  <div class="container-fluid bg">
    <h2>Formulaire tag_form</h2>
    <%= form_tag action= "/new", method: "post" do %>
      <%= label_tag 'Username' %>
      <%= text_field_tag 'username', @user.username %>
      <br /> <br />

      <%= label_tag 'Email' %>
      <%= text_field_tag 'email', @user.email %>
      <br /> <br />

      <%= label_tag 'Bio' %>
      <%= text_area_tag 'bio', @user.bio %>
      <br /> <br />

      <%= submit_tag "Create User" %>

    <% end %>
    </form>
  </div>
    
</section>
-->
<section>
  <div class="container-fluid bg">
    <h2>Formulaire form_for</h2>
    <%= form_for @user, url: {action: "create"}, html: {class: "nifty_form"} do |f| %>
      <%= f.text_field :username %>
      <%= f.text_field :email %>
      <%= f.text_area :bio, size: "60x12" %>
      <%= f.submit "Create" %>
    <% end %>
  </div>
</section>